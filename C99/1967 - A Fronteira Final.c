#include <stdio.h>

int main(){

    int G, A, M, C;

    /*
    G = Número de cursos de graduação
    A = Número total de tipos de atividade
    M = Número total de tipos de material
    C = Número de camp da unviersidade
    */

    scanf("%d %d %d %d", G, A, M, C);
    if((G >= 1 && G <= 600) && (A >= 1 && A <= 100) && (M >= 1 && M <= 2000) && (C >= 1 && C <= 100)){
        printf("\n");
        int matriz[G][A];





    }







    return 0;
}